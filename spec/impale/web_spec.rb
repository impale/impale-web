require 'spec_helper'

describe Impale::Web do
  it 'has a version number' do
    expect(Impale::Web::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
